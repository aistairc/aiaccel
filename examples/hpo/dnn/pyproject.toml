[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "task_for_integration_example"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torchvision"
]

[tool.setuptools]
package-dir = {"" = "task_for_integration_example"}
